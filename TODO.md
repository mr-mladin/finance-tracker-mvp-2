# TODO: Finance Tracker MVP 2

## Ближайший фокус

1. `План`
   - отдельный блок просроченных планов;
   - действие `Отметить фактом`;
   - перенос плановой операции;
   - прогноз свободных денег до конца месяца.

2. Данные
   - импорт из файла вместо `prompt`;
   - экспорт с версией схемы;
   - проверка JSON перед заменой данных;
   - предупреждение перед импортом.

3. Подготовка к Supabase
   - отделить слой данных от рендера;
   - описать миграцию localStorage -> Supabase;
   - не подключать Supabase, пока local MVP не стабилен.

## Сделано

- `Отчёт`: сравнение с прошлым месяцем, топ-5 расходных категорий, доходы / расходы / итог и пустые состояния.

## Технический долг

- `styles.css` всё ещё крупный; старые навигационные хвосты частично вычищены, но файл стоит постепенно дробить по смысловым блокам.
- `app.js` содержит и state, и render, и handlers; перед Supabase лучше вынести data/store-логику.
- Нет автотестов; пока минимум проверки - `node --check app.js` и браузерные сценарии.
